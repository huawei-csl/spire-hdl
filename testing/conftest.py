# These tests require the `flowy` package which is only available locally,
# not in CI. They can still be run manually with `python <script>`.
collect_ignore = [
    "test_flowy_optimized_decorator.py",
    "test_flowy_optimized_decorator2.py",
]
