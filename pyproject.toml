[build-system]
requires = ["hatchling", "hatch-vcs", "hatch-requirements-txt"]
build-backend = "hatchling.build"

[project]
name = "spire-hdl"
version = "0.1.0"
description = "SpireHDL is a hardware description language for digital circuits."
authors = [{ name = "Felix Arnold", email = "felix.arnold@h-partners.com" }]
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dynamic = [
    "dependencies"
]

# Tell hatchling what to put into the wheel
[tool.hatch.build.targets.wheel]
packages = ["src/spirehdl"]
include = [
    "src/spirehdl/arithmetic/int_multipliers/data/**",
    "src/spirehdl/arithmetic/eval/*.json",
    "src/spirehdl/arithmetic/eval/*.csv",
]

# Optional but nice: also define sdist the same way
[tool.hatch.build.targets.sdist]
packages = ["src/spirehdl"]
include = [
    "src/spirehdl/arithmetic/int_multipliers/data/**",
    "src/spirehdl/arithmetic/eval/*.json",
    "src/spirehdl/arithmetic/eval/*.csv",
]

[tool.hatch.metadata.hooks.requirements_txt]
files = ["requirements.txt"]

[tool.ruff]
line-length = 120
